The model is now outdated and doesn't match the code.
  (for example, there is only a mention that there might be distances.)
I will fix this before making the repo public.







This code needs some sort of tutorial.


First of all, in matlab go to the 'matlab' folder.
Then run 'env'.


You should now be able to call any of the other functions.
