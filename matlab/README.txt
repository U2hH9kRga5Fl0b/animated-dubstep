Here are roughly what the folders contain:


example: some example code to see how this works
export:  the functions we expect other teams to use
src:     source code to break parts of the simulator down
test:    code to test the simulator
objects: the object model