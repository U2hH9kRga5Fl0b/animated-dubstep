
% This function is future work.

% This function should add the differences between when requests are serviced and when they were supposed to be serviced.

% c is the city
% sol is the solution
% times is the time that each stop is visited (output from get_all_times)


%   author <not assigned>

function [time_violations] = count_time_violations(c, sol, times)

time_violations = zeros(size(times));

end