% Returns true when all the times that each action is performed at fall within their time windows.



% author     <not assigned>

function [is_valid] = satisfies_time_windows(c, sol)

is_valid = true;

end